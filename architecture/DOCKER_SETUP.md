# Docker Setup Guide for E-Dukan

This file has been merged into the canonical runtime documentation.

Use [09-runtime-and-deployment.md](./09-runtime-and-deployment.md) for:

- Docker build strategy
- Coolify setup notes
- environment variables and ports
- migration and runtime behavior
- deployment checks

The container and deployment guidance from this legacy file has been folded into that canonical document so runtime assumptions live in one place.

