/**
 * Re-export from canonical location.
 * Many resource files import from './resource.types' — this file bridges to the base module.
 */
export { defineResource, type ResourceDefinition } from './base/resource'
