{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "enabledPlugins": {
    "superpowers@claude-plugins-official": true,
    "typescript-lsp@claude-plugins-official": true
  },
  "permissions": {
    "allow": [
      "Bash(pnpm *)",
      "Bash(pnpm)",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(npx *)"
    ],
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(./**/.env)",
      "Read(./**/.env.*)",
      "Read(./secrets/**)",
      "Bash(git push --force*)",
      "Bash(git reset --hard*)"
    ]
  },
  "companyAnnouncements": [
    "ERP team: read docs/ai-engineering.md and docs/superpowers/README.md before large features.",
    "Non-trivial work needs an approved spec in docs/superpowers/specs/ before implementation.",
    "Use /caveman or the caveman skill for full orient → brainstorm → spec → implement → review cycles.",
    "Golden reference for CRUD: units vertical slice (see AGENTS.md)."
  ]
}
